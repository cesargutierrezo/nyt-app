describe("ArticleList", () => {
  it("renders article cards", () => {});

  describe("when search query is empty", () => {
    it("renders suggested heading", () => {});
  });

  describe("when search query exists", () => {
    it("does not render suggested heading", () => {});
  });
});
